apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: restaurant-app
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend  # matches both v1 and v2 deployments
  ports:
    - name: http
      port: 80
      targetPort: 80
